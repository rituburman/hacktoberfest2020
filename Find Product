#include<stdio.h>
#include<math.h>
int main()
{
    int n,arr[100000],i;
    long long int ans=1;
    int mod=pow(10,9)+7;
    scanf("%d",&n);
    for(i=0; i<n; i++)
    {
        scanf("%d",&arr[i]);
        ans=(ans*arr[i])%mod;
    }
    printf("%d\n",ans);
    return 0;
}
